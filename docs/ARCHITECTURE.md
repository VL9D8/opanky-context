---
schema_version: 1
document_id: architecture
owner: OPANKY
status: active
last_verified_at: 2026-08-10
---

# Architecture

## Source ownership

This hub owns cross-project public context. Each project repository owns code and runtime truth. External design systems own original assets. Git history owns accepted evidence. Private capture remains separate.

Owning implementation repositories may remain private. The public hub records verified project-level state and approved public proof URLs, but it does not expose private repository URLs merely to make the ecosystem appear connected.

## Boundaries

Private capture -> curated change -> public hub -> owning project repositories. The hub summarizes verified public context; it does not replace source repositories or external asset owners.

Verified owning-repository evidence may also enter the private-by-default Content Engine, where a privacy review, ranked ideas, drafts, and explicit feedback remain separate from publication. The public hub records only safe project-level state; it does not store private drafts or source material. Publication remains a separately authorized action.
